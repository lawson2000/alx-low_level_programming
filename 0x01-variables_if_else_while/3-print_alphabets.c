#include <stdio.h>
/**
 * main - print alphabet in lower and upper case
 *
 * Return: (0) success
 */
int main(void)
{
	char ch;
	for (ch ='a' ; ch <= 'z' ; ch++)
	{
		char ch;
	}
	for (ch = 'A' ; ch <= 'Z' ; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return(0);
}
