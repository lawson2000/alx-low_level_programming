#include <stdio.h>
/**
 * main - print all single digit numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int digit;

	for (digit = 0; digit <10; digit++)
		printf("%i\n", digit);
	return (0);
}
